#!/usr/bin/env python

from setuptools import setup, find_packages

setup(
    name='dailybackup',
    version='1.0',
    description='STC Stores daily backup',
    author='Luke Shiner',
    packages=find_packages(),
    )
